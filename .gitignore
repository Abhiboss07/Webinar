# Dependencies (regenerated via `npm install`)
node_modules/

# Secrets — NEVER commit real env files (only .env.example is tracked)
.env
.env.*
!.env.example

# Build artifacts / archives (regenerable)
*.zip
dist/

# Locally-uploaded media (dev only; prod uses Cloudinary)
backend/uploads/

# Logs / OS / editor cruft
*.log
.DS_Store
Thumbs.db
.vscode/
.idea/

# Generated migration report (regenerated by npm run migrate:config)
backend/scripts/migration-report.json
